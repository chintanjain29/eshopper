<style>
    .custom-content {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 300px; /* Minimum height */
    }
  
    .custom-heading {
        font-weight: bold;
        text-transform: uppercase;
        margin-bottom: 1.5rem; /* Adjust margin bottom as needed */
    }
  
    .custom-breadcrumb {
        display: inline-flex;
    }
  
    .custom-breadcrumb a {
        text-decoration: none; /* Remove default underline */
        color: inherit; /* Inherit text color */
        transition: color 0.3s, border-bottom-color 0.3s; /* Smooth transition for color and underline */
    }
  
    .custom-breadcrumb a:hover {
        color: #D19C97; /* Change color on hover */
        border-bottom: 1px solid #D19C97; /* Underline on hover */
    }
    .wishlist-row {
      display: flex;
      flex-wrap: wrap;
      margin: -15px; /* Increased negative margin to counteract increased padding on grid items */
  }
  .wishlist-grid {
      border: 1px solid #e0e0e0; /* Lighter border color */
      width: calc(25% - 30px); /* Adjusted for increased margins */
      margin: 15px;
      padding: 20px;
      box-sizing: border-box;
      background-color: #ffffff; /* White background */
      box-shadow: 0 2px 5px rgba(0,0,0,0.05); /* Subtle shadow for depth */
      transition: box-shadow 0.3s ease; /* Smooth transition for hover effect */
  }
  .wishlist-grid:hover {
      box-shadow: 0 5px 15px rgba(0,0,0,0.1); /* Increased shadow on hover */
  }
  .wishlist-grid a {
      text-decoration: none;
      color: inherit; /* Ensures text color matches your theme */
  }
  .wishlist-grid img {
      max-width: 100%;
      height: auto;
      margin-bottom: 15px; /* Space between image and text */
  }
  .wishlist-grid h4 {
      margin: 10px 0; /* Spacing between title and price */
      font-size: 1rem; /* Adjust as needed */
  }
  .wishlist-grid h4:last-child { /* Price */
      color: #fff; /* Assuming this matches your theme's accent color */
      font-weight: bold;
  }
  
  /* Responsive adjustments */
  @media (max-width: 991px) {
      .wishlist-grid {
          width: calc(33.333% - 30px); /* 3 items per row on medium screens */
      }
  }
  @media (max-width: 767px) {
      .wishlist-grid {
          width: calc(50% - 30px); /* 2 items per row on small screens */
      }
  }
  @media (max-width: 479px) {
      .wishlist-grid {
          width: calc(100% - 30px); /* 1 item per row on very small screens */
      }
  }
</style>


<div class="page-width">
  <div class="custom-container bg-secondary mb-5">
      <div class="custom-content">
          <h1 class="custom-heading">{{ page.title }}</h1>
          <div class="custom-breadcrumb">
              <p>{% render 'breadcrumb' %}</p>
          </div>
      </div>
  </div>
  <div class="wishlist-row">
      <!-- Wishlist items will be appended here -->
  </div>
</div>

<script>
  $(document).ready(function(){
      var wishlistRow = $('.wishlist-row');
      {% for product in collections.all.products %}
          var wishid = localStorage.getItem("wishId-{{ product.id }}");
          if(wishid == '{{ product.id }}'){
              var wishGridItem = `
                  <div class="wishlist-grid">
                      <a href="{{ product.url }}">
                          <img src="{{ product.featured_image | image_url: width: 250, height: 250, crop: 'center' }}" alt="{{ product.title }}">
                          <h4>{{ product.title }}</h4>
                          <h4>{{ product.price | money }}</h4>
                      </a>
                  </div>
              `;
              wishlistRow.append(wishGridItem);
          }
      {% endfor %}
  });
</script>